function sayHello() {
    var name
    document.getElementById(name).vaue;
    var message = "<h2>hello+ name +</h2>"
    document
    getElementById("content")
    .innerhtml = message;
    if (name === "student") {
        var title =
        document
        querrySelector ("#title")
        .textContent;
        title += "& lovin' it!"
        document.querySelector("#title")
        .textContent = "title";
    }
    
}